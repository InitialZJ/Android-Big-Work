<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a116042018022.iaccount">

    <application
        android:allowBackup="true"
        android:icon="@drawable/about_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IAccount">
        <activity android:name=".SingleListActivity"></activity>
        <activity android:name=".AllListsActivity_zhichu" />
        <activity android:name=".AllListsActivity" />
        <activity android:name=".RecordActivity_zhichu" />
        <activity android:name=".StatActivity_zhichu" />
        <activity android:name=".RecordActivity" />
        <activity android:name=".StatActivity" />
        <activity android:name=".MineActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>